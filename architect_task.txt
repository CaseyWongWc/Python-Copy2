Read the architect skill at .local/skills/code_review/SKILL.md and use it to review my recent changes to sandbox/inline_output_v7.py and sandbox/_smoke_test_v7.py. The changes implement three Casey-requested fixes: (a) strip legacy # RUN out: / # RUN err: lines via a new _RUN_PREFIX_RE applied in both _strip_old_annotations and _strip_for_clean_save (around lines 135-188); (b) wrote phone cheat sheet at ZYBOOKS/C13/CHEATSHEET.MD; (c) line-aware # in: queue: _parse_all_inputs (lines ~1283-1380) now returns (line_no, batch_id, value) triples, batching consecutive # in: lines (only blanks/comments between), opening a new batch when real code intervenes. The shim's _piped_input (lines ~1444-1485) picks the most recent reachable batch (line<=current), drops older reachable batches, and falls back to legacy FIFO if nothing reachable. Also updated the RUN-block stdin parsing at line ~1812 to flatten the new tuple format. All 10 v7 smoke tests pass (added 3 regression tests: stale-input shadowing, multi-input batch, RUN-prefix strip), all 5 baseline smoke tests pass, and the workflow now runs goog.py end-to-end clean. Please call architect with includeGitDiff=true and report back any severe issues. Do NOT modify code; just report architect's findings verbatim.
